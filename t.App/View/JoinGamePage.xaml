<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="t.App.View.JoinGamePage"
             Title="{Binding Path=Title}"
             BackgroundColor="{DynamicResource SecondaryColor}">
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1" Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*" />
                    <ColumnDefinition Width="5*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Text="Server Ip" Grid.Column="0" Grid.Row="0" />
                <Entry Text="{Binding Path=ServerIp}" Grid.Column="1" Grid.Row="0" />

                <Label Text="Server Port" Grid.Column="0" Grid.Row="1" />
                <Entry Text="{Binding Path=ServerPort}" Grid.Column="1" Grid.Row="1" />

                <Label Text="Player name" Grid.Column="0" Grid.Row="2" />
                <Entry Text="{Binding Path=PlayerName}" Grid.Column="1" Grid.Row="2" />

                <Button Text="Join Game" 
                        Command="{Binding Path=JoinGameCommand}"
                        Grid.Column="1" Grid.Row="4"></Button>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>